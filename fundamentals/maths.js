const num1=100;
const num2=50;

let val;

//Math has a number of properties and functions we can use to perform mathematical calculations

val=Math.PI;
val=Math.round(2.4);
val=Math.ceil(2.4);
val=Math.floor(2.8);
val=Math.sqrt(64);
val=Math.abs(-3);
val=Math.pow(8,2);
val=Math.min(2,23,23,2,1,1,2,2,-1);
val=Math.max(2,2,1,2,34,4,45,24);

val=Math.floor(Math.random() * 20+1);

console.log(val);
