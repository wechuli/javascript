(function() {
  "use strict";
  function greetMe(yourName) {
    console.info(`Hello ${yourName}`);
  }
  greetMe("Wechuli");
})();


// var - declares a variable, optionally initializing it to a value
// let decalres a block-scoped, local variable, optionally initializing it to a value
//const decalres a block-scoped, read-only named constant


//You use variables as symbolic names for values in your application

//ECMAScript 2015 let(const) does not hoist the variable to the top of the block