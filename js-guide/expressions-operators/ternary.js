//consition ? val1 : val2
// if the consition is true, the operator has the value of val1. Otherwise it has the value of val2

let age = 120;

let status = age >= 18 ? "adult" : "minor";
console.log(status);
